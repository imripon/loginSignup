import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shortsn.test',
  appName: 'status bar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
